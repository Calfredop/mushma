---
kind: task
title: Refresh the README with the latest implementations
status: Done
complexity: trivial
---
Bring `README.md` up to date with what landed on `main` since the last README refresh (commit 6cc2650): the growth clock, terrain microclimate and the sun-exposure and slope stoppers (card `algo-more-factor`), the verbose "why this score" panel (card `verbose-score-explanation`), the floating centre-on-my-position button, the porcino app icon, the `/status` and `/species` routes, the rain rescale in `model.yaml`, the finished 2016–2025 weather backfill, the SIR gauge check command, and the validation doc's newer findings. Claim only what is on `main`: the backfill's client fixes and the 2019–2025 gauge numbers are still uncommitted in the backend worktree.
