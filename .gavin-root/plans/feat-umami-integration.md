---
complexity: moderate
order: 8192
kind: plan
title: [feat] umami integration
status: In Progress
---
Self-hosted Umami analytics for mappafunghi.app, to see how the group and portfolio
visitors use the app. Traces to PRD → Architecture (backend on the Hetzner box, under
€10/month) and Principles → Sightings privacy.

**Decided (don't re-litigate):**
- Self-hosted on the Hetzner CX23: Umami + Postgres in `deploy/compose.yaml`, behind
  Caddy at `m.mappafunghi.app` ("DNS only" on Cloudflare, like `api.`).
- Consent-gated: the script loads only once the visitor accepts the cookie banner that the
  "Terms and conditions + privacy" card builds (`web/src/consent.ts`,
  `mushma.cookieConsent.v1` = `accepted`), and it follows the choice live. Accept loads it.
  Decline stops it at once, because the before-send guard drops everything unless consent
  is `accepted`. Tracks pageviews (`/`, `/credits`) and these events:
  species and view (Now/Seasons/Outlook) switches; spot opened, with its method (`map`,
  `search`, `gps`, `hotspot`); date moves, as the offset from today (`-6`…`+7`) or
  `replay`; PWA install accepted; language switched.
- **Never send a location.** The URL carries `?at=lat,lon` at ~1 m and `?cell=`
  (`web/src/state/urlState.ts`), and by default Umami records every
  `pushState`/`replaceState` URL. So: `data-exclude-search` on the script, plus a
  `data-before-send` guard that strips any query string from `url` and `referrer` and
  drops any event property not on an allowlist. Events never carry coordinates, cell
  ids, comune codes, place names or dates.
- The tracker script and collect endpoint get neutral names (none of `umami`, `track`,
  `stats`, `analytics`, `collect`, `send`, `event`) so generic blocklists miss them. Do
  Not Track is not honoured.
- Only production counts: the script loads only in a production build with the env vars
  set, with `data-domains=mappafunghi.app`, so previews, dev, tests and the tunnel send
  nothing.
- Out of scope: the cookie banner, its copy and the consent store (the T&C card owns
  them), privacy/legal copy (the "Terms and conditions + privacy" card covers Umami),
  backups of the Umami DB (the analytics data is disposable), custom dashboards/reports.

**Order:** the T&C card's cookie banner and `web/src/consent.ts` land before item 3 here.
Production never gets the Umami env vars before the banner is live, so Umami never runs
ungated.

**Done:** a visit to mappafunghi.app, including one opened with `?at=…`, shows up in the
Umami dashboard as `/` with no query string once the banner is accepted, and a declined
visit sends nothing. Every event arrives with only its allowed properties. The daily job
still passes with Umami running.

- [x] Spike: pin a current Umami release (and Postgres major) from the official docker-compose. Confirm whether the pinned image applies `TRACKER_SCRIPT_NAME` and `COLLECT_API_ENDPOINT` at runtime or only at build time, and that its tracker supports `data-exclude-search` and `data-before-send`, and whether a before-send hook returning a falsy value drops the payload (the decline switch). Any rename that only works at build time falls back to Caddy rewrites. Write down what you found in this card before going on.
  - **Findings (2026-09-23):** Pin `ghcr.io/umami-software/umami:3.4.0` (current release, published 2026-09-17; same digest as `latest`/`postgresql-latest`, so it's already the Postgres-flavored image — no `DATABASE_TYPE` needed) with `postgres:15-alpine` (what Umami's own `docker-compose.yml` pins at this tag). `TRACKER_SCRIPT_NAME` (since v1.26.0) and `COLLECT_API_ENDPOINT` (since v1.34.0) are both **runtime** env vars read when the container starts, not build-time — so `deploy/umami.env` alone renames the script path and collect endpoint, no Caddy-rewrite fallback needed. The tracker supports `data-exclude-search` (strips the URL search string before send) and `data-before-send`: the hook receives `(type, payload)`, and **returning a falsy value cancels the send** — confirmed, this is the decline switch. `payload` carries `hostname, language, referrer, screen, title, url, website`, plus `name`/`data` for `track()` custom events, which is what `analytics.ts`'s guard strips/allowlists. `data-domains` matches `window.location.hostname` client-side (not just a server check), reinforcing the production-only gate.
- [x] Server stack in the repo: `umami` and `umami-db` services in `deploy/compose.yaml` (pinned images, healthcheck + `depends_on`, `mem_limit`, the same log rotation, `DISABLE_TELEMETRY=1`, a named DB volume); secrets in a gitignored `deploy/umami.env` with a committed `umami.env.example`; a `m.mappafunghi.app` site in `deploy/Caddyfile`. Check: `docker compose config` passes, and locally the tracker is served at its renamed path.
  - **Done (2026-09-23):** `umami` (`ghcr.io/umami-software/umami:3.4.0`, 512m) + `umami-db` (`postgres:15-alpine`, 256m, `umami_db_data` volume) added to `deploy/compose.yaml`, sharing one `deploy/umami.env` (`APP_SECRET`, `POSTGRES_USER/PASSWORD/DB`, `DATABASE_URL`) — `deploy/umami.env.example` committed, `deploy/umami.env` added to `.gitignore` (the blanket `.env`/`.env.*` patterns don't catch a file with no leading dot). `TRACKER_SCRIPT_NAME=/js/widget.js` and `COLLECT_API_ENDPOINT=/api/hub` are set directly in compose (not secret) alongside `DISABLE_TELEMETRY=1`. `m.mappafunghi.app` added to `deploy/Caddyfile`, "DNS only" like `api.`, reverse-proxied to `umami:3000`, `X-Robots-Tag noindex`. `docker compose config` passes. Ran `umami`+`umami-db` locally: both went healthy; `GET /js/widget.js` returns 200 with the real tracker JS (byte-identical to `/script.js`, which Umami leaves live too — renaming *adds* the alias, it doesn't close the default path, so this only matters if our own frontend requests the wrong one); `POST /api/hub` and the default `/api/send` both 400 on an empty body (routed the same; a bogus path 404s for contrast). The frontend must request `/js/widget.js` / `/api/hub`, never the defaults.
- [x] `web/src/analytics.ts`, test first. `initAnalytics()` (called from `main.tsx`) injects the script only when `import.meta.env.PROD` is true, `VITE_UMAMI_SRC` + `VITE_UMAMI_WEBSITE_ID` are set and consent is `accepted`, and it subscribes to `onConsentChange()` so a later Accept loads it. The script carries `data-exclude-search`, `data-domains` from `VITE_UMAMI_DOMAINS`, and the before-send guard, which drops everything unless consent is `accepted`, strips query strings and drops unknown keys. `track()` takes a typed event union and does nothing when the tracker isn't loaded. Vitest covers: no script when unconfigured or without consent, loading on Accept, the attributes, and the guard dropping on decline, stripping queries and dropping unknown keys.
  - **Done (2026-09-23):** `data-auto-track="false"` too — Umami monkey-patches `pushState`/`replaceState` itself, and every in-app move (species/view switch, date change) is one of those (`usePath`), so left on it would auto-fire a URL-carrying pageview on every one of them. Instead the script's own `load` handler fires the session's one manual `window.umami.track()` pageview, and everything else is a typed `track(event)` call from the UI. The guard runs on every send (pageview or event): drops it unless `getConsent() === 'accepted'`, collapses `url` to `/` (or `/credits`, the only two pageviews the plan lists — `/terms` and `/privacy` collapse to `/` too, undecided whether they deserve their own bucket later), strips the referrer's query string, and keeps only allow-listed `data` keys (`species, view, method, offset, lang`) — this also scrubs any stray `at=`/`cell=` an *event* payload's own `url` field would otherwise carry, not just pageviews. 12 Vitest cases, `tsc --noEmit`, `lint` and `format:check` all pass.
- [x] Wire the events where the actions happen (species/view handlers in `App.tsx`, map tap, `PlaceSearch`, `useLocate`, hotspot select, `DateStrip`/`TimeBar`/replay, `InstallBanner`, `LanguageSwitcher`), plus one light test that a UI action calls `track` with the right payload. `pnpm test`, `lint`, `format:check` and `build` pass.
  - **Done (2026-09-23):** `main.tsx` calls `initAnalytics()`. `App.tsx`: `openSpot` (the single funnel all five spot-opening paths already went through — map cell/point tap, `PlaceSearch`, `useLocate`'s GPS fix, hotspot select from the map or the Hot Places list) now takes a `method` and tracks `spot-open`; `handleSpeciesChange` (used by both `SpeciesSwitcher` and the Outlook panel's species picker) tracks `species-switch`; the `ViewTabs` `onChange` tracks `view-switch`; `handleDateChange` (used by both `TimeBar`'s `onDate` — which is itself `DateStrip`'s and the prev/next/today buttons' single funnel — and `SeasonsPanel`'s `onReplayDay`) computes the offset from today and tracks `date-move`, `'replay'` once it's further back than the date strip's window. `usePwaInstall`'s `install()` tracks `pwa-install` only when `userChoice` resolves `accepted`. `LanguageSwitcher` tracks `language-switch` before calling `changeLanguage`. Tests: the species-switch path in `App.test.tsx` (the one light UI test), plus two in `usePwaInstall.test.tsx` since that hook's own branching changed (track vs. not, by outcome) — a bit more than "light" but each asserts real new logic, not incidental wiring. 283 Vitest tests, `tsc -b`, `lint`, `format:check` and `build` all pass. `tsc -b` (the build's actual type-check) caught two things plain `tsc --noEmit` had missed: `vi.stubEnv('PROD', …)` wants a real `boolean`, not a casted string, and `openSpot`'s new signature needed `CameraRequest` imported as a type.
- [x] Local end-to-end: Umami running locally; `pnpm build && pnpm preview` with the env vars pointed at it; open a `?at=…&species=ovoli` link, switch species, change the day, open a spot by search. In Umami's Postgres, `website_event` has no URL query and no referrer query, exactly one pageview, and each event carries only its allowed keys. Decline first: no rows at all. Then Accept: rows start.
  - **Found and fixed a real bug (2026-09-23):** with `TRACKER_SCRIPT_NAME=/js/widget.js` (item 2's original choice), Umami 3.4.0's tracker resolves `COLLECT_API_ENDPOINT` **relative to the script's own directory**, not the site root — every send went to `.../js/api/hub` (404/503) instead of `.../api/hub`, so nothing was ever recorded. Fixed by moving the tracker script to the server root (`TRACKER_SCRIPT_NAME=/widget.js`, `deploy/compose.yaml` and its comment updated); `COLLECT_API_ENDPOINT=/api/hub` then resolves correctly since the script's directory is `/`. Re-verified locally after the fix.
  - **Done:** local `umami` + `umami-db` (port published only for this test, not in the committed compose), a website created via Umami's API (`POST /api/websites`, domain `localhost`), `pnpm build` with `VITE_UMAMI_SRC/WEBSITE_ID/DOMAINS` pointed at it, `pnpm preview`, driven with `claude-in-chrome`. Declined first: 0 rows in `website_event` through a species switch, a date change and a map click. Accepted: exactly one pageview row (`url_path='/'`, no query, no referrer) plus one row each for `view-switch`, `language-switch`, `species-switch`, `spot-open` (`method=search`, via `PlaceSearch`) and `date-move` (`offset=1`), every one `url_path='/'` with no query even though the browser's address bar carried `?at=…&view=seasons` throughout — each event's own `data` row held exactly its one allowed key, nothing else. (Note: the PWA service worker precaches the app shell, so a rebuilt `dist/` needs `caches.delete` + SW unregister to actually reload in the same browser profile — expected for a PWA, not a bug.) Didn't reach the `'replay'` offset branch or the `gps`/`hotspot`/`map` spot methods live (no local API/season data to drive them to); that logic is identical code to what was verified, just a different literal.
- [x] Docs: README → Deploying (table row, Umami setup, the web env vars set on Vercel **Production only**), `web/.env.example` (commented out), PRD → Architecture (self-hosted Umami and why) and PRD → Principles (analytics never receives a location).
  - **Done (2026-09-23):** README → Deploying: a cost-table row, the 3 `VITE_UMAMI_*` vars under **web/ → Vercel** with a note on *why* Production-only actually matters (a Preview deployment is still `import.meta.env.PROD === true`; only the env vars being unset keeps it off), and an **Analytics → Umami, same box** section under **api/ → Hetzner** (secrets, first-run login/password-change/website-creation, and the `TRACKER_SCRIPT_NAME`-at-root gotcha from item 5). `web/.env.example`: the 3 vars, commented out. PRD → Architecture: a new bullet on self-hosted Umami and why (visibility without cost or a third party). PRD → Principles: a new bullet, "Analytics never receives a location" (the Architecture bullet points at it instead of misciting Sightings privacy, which is about a different kind of location — public occurrence records, not visitor taps).
- [x] Commit only the files you touched. Then **stop and ask the human to push** (or to OK a push): the server pulls from GitHub and Vercel deploys from `main`.
  - **Done (2026-09-23):** commit `49d1470` on `feat/umami`, 15 files (12 modified, 3 new) — exactly the ones touched, nothing else staged. Working tree clean afterwards.
- [ ] Rollout. Ask the human for a Cloudflare `A` record `m` → the server's IP, DNS only (and for the host, if you don't have it). Then over SSH as root: `git pull`, create `deploy/umami.env` with generated secrets, `docker compose up -d`, wait for Caddy's certificate. Log in, replace the default `admin`/`umami` password (give it to the human in chat, never write it to a file), and create the website "Mappa Funghi" / `mappafunghi.app`. Ask the human to set `VITE_UMAMI_SRC`, `VITE_UMAMI_WEBSITE_ID` and `VITE_UMAMI_DOMAINS` on Vercel (Production only) and redeploy, only once the cookie banner is live in production.
- [ ] Server headroom: with Umami up, run `systemctl start mushma-daily` once. It must pass (`journalctl -u mushma-daily`). Note the peak `free -m` and `docker stats` figures in this card.
- [ ] Live check: open `https://mappafunghi.app/?at=43.5,11.2`, accept the banner and trigger each event. The dashboard shows `/` with no query and the events with their properties, and a declined visit and a Vercel preview deployment send nothing. Record the result here.

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
